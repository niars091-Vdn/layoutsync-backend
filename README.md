# LayoutSync Backend

API FastAPI per rilievo tecnico con AI.

## Endpoints
- GET /app → PWA
- GET /demo → demo output ZIP
- POST /analizza → analisi point cloud
- GET /status → health check
- GET /docs → Swagger UI
